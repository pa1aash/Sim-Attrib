\documentclass{article}

\usepackage[dblblindworkshop]{neurips_2026_template/neurips_2026}
\workshoptitle{Sim2Science}

\usepackage[utf8]{inputenc}
% U+02BC (modifier letter apostrophe) appears once in audit/BIBLIOGRAPHY.bib, in
% Crossref's own fetched record for Brynjarsdottir & O'Hagan (2014), not in
% inputenc's default utf8 table. Declared here rather than edited in the .bib,
% which stays exactly as fetched (audit/BIBLIOGRAPHY.bib's own stated policy).
\DeclareUnicodeCharacter{02BC}{\textquoteright}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
% \title{} alone doesn't populate PDF metadata in this document class, so pdftitle is set
% explicitly here; pdfauthor is deliberately left unset for anonymity.
\hypersetup{pdftitle={Diagnostics and Limits for Component-Level Simulator Misspecification}}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{longtable}
\usepackage{placeins}

% Page-limit closure (T1-2/T2-6 carryover, G12): float separation tightened, not font size or
% figure width; the reverted G11 fix shrank effective on-page font size below the venue floor
% by shrinking \includegraphics width; this changes only inter-float whitespace.
\setlength{\textfloatsep}{1pt plus 2pt minus 1pt}
\setlength{\floatsep}{1pt plus 2pt minus 1pt}
\setlength{\intextsep}{1pt plus 2pt minus 1pt}
\setlength{\abovecaptionskip}{1.5pt}
\setlength{\belowcaptionskip}{0.5pt}
% Same closure (G13): the template's own \parskip (5.5pt, applied between every paragraph)
% tightened by 1.5pt (again pure inter-paragraph whitespace, no font size or content change).
% G14: tightened a further 1.5pt (to 2pt) to offset this session's added findings-list,
% ellipsoid-computation, and mechanism-naming prose (R14-1/R14-2), same lever, no font size or
% content change.
% G16: tightened a further 0.5pt (to 0.5pt) to offset the measured Montel-comparison result and
% the Fithian/Lee citation sentence added to Section 2 (W16-2/W16-10), same lever.
\setlength{\parskip}{0.5pt plus 1pt minus 1pt}

% Title: diagnostics-and-limits framing, matching the paper's own argument. No
% em-dash, no colon.
\title{Diagnostics and Limits for Component-Level Simulator Misspecification}

\author{%
  Anonymous Author(s) \\
  Anonymous Institution \\
  Anonymous Address \\
  \texttt{anonymous@example.com} \\
}

\begin{document}

\maketitle

\begin{abstract}
Attributing a simulator's misspecification to one component presupposes that the available
summaries can tell the components apart, a precondition that can fail quietly. We check it,
on a three-component epidemic simulator under an adversarial threat model, using
rank-and-condition-number diagnostics. One summary set separates all three components under
every one of eight distortion assignments tried, given at most one distortion parameter per
component; the same measurement locates exactly where this breaks once a component carries two
parameters at once ($\kappa=628.9$, rank 4 of 6), on a real, interpretable confound. A
selective-inference construction for a calibrated attribution test is affordable at a known
parameter but fails to terminate once the nuisance parameters are known only to the precision a
maximum-likelihood fit gives them, a box wider, on every coordinate, than one already shown to
break it. The boundary is predicted before it is
measured, from the ratio of nuisance shift to observation noise, and the prediction holds.
Every tool used is prior art; the contribution is the finding.
\end{abstract}

\section{Introduction}
\label{sec:intro}

A simulator used for scientific inference is essentially always misspecified somewhere.
Existing SBI tests ask whether it is misspecified at all; a practitioner who gets a positive
answer usually wants to know \emph{which part} to fix. Attributing misspecification to a
specific component presupposes something the detection question does not need: that the
available summary statistics can actually tell the components apart. That precondition can fail
quietly, and a procedure that reports a confident attribution without checking it first is
reporting a number the data may not support.

This paper checks that precondition on a canonical case, a three-component compartmental
epidemic simulator with transmission, progression, and observation as the components, and
reports four findings: separability holds under every
one-parameter-per-component assignment tried; it fails once a component carries two, on a real
confound; a calibrated attribution test is affordable at a known parameter; and it fails to
terminate once nuisance parameters are known only to data-implied precision, predicted before
measured (Table~\ref{tab:summary}, Appendix~\ref{sec:appendix}, gives an overview with evidence
figures).

\section{Background and related work}
\label{sec:background}

\begin{itemize}[topsep=1pt,itemsep=0pt,parsep=0pt,leftmargin=*]
\item \textbf{Detecting misspecification versus attributing it.} SBI has moved from asking
whether a simulator is misspecified to asking where: \citet{Cranmer_2020} name summary
informativeness and misspecification diagnosis among the field's open problems.
\citet{schmitt2022detectingmodelmisspecificationamortized} flag out-of-distribution embeddings
with one discrepancy score; \citet{ward2022robustneuralposteriorestimation} model the
summary-level error explicitly; \citet{Frazier_2020} give a shrinking-tolerance ABC diagnostic
for posteriors concentrating under misspecification. Each is a global or per-summary signal
that something is wrong, not which component caused it, the question this paper asks instead.
\citet{montel2025testsmodelmisspecificationsimulationbased}'s family of tests, our baseline,
corrects for multiplicity via a trials-corrected $p$-value on the \emph{minimum} of per-summary
statistics: the global null that something is misspecified, not the identity of the arg\,min. We
run their global test on our simulator (exact per-summary statistics replace their neural ones,
the only simplification; Appendix~\ref{sec:claims-ledger}): it rejects at both declared corners
and at two realizations of the six-column confound's named mechanisms ($p\approx0.004$; a
null-data control does not reject, $p=0.61$). Its arg\,min flags disjoint coordinates for the two
mechanisms at this magnitude, contradicting our earlier unmeasured claim --- one realization
each, not a distribution, and a separate question from the Jacobian's \emph{local} near-null
ambiguity at $\eta=0$ (Section~\ref{sec:experiments}).
\item \textbf{The identifiability precondition.} \citet{Kahl_2019} answer it exactly in a
closely related setting: per-component input reconstruction in a dynamic system is identifiable
iff a structural sensitivity map has full column rank, resting on \citet{Sain_1969}'s
invertibility condition. \citet{CatchpoleMorgan_1997} give the general parameter-redundancy
framework this instantiates; \citet{Brynjarsd_ttir_2014} frame the broader model-discrepancy
problem for computer-model calibration; \citet{Miao_2011} give the differential-algebra route
on epidemic-ODE models directly (two parameters entering only through a fixed combination are
non-identifiable regardless of design, exactly our six-column confound's failure mode), and
\citet{Raue_2009}'s profile likelihood is the numerical diagnostic for the same failure under
noisy data.
\item \textbf{Diagnostic tooling.} \citet{Cintr_n_Arias_2009} screen a sensitivity matrix's rank
and condition number to decide whether parameters are jointly recoverable, on a compartmental
epidemic model, the class we use in Section~\ref{sec:experiments}. Our Jacobian is simulation-estimated rather than symbolic, so we
also need to know how far a finite-difference estimate can be trusted under simulation noise;
\citet{Mor__2011} and \citet{Mor__2012} give the relevant results, of which our step-size sweep
(Section~\ref{sec:method}) is a coarser, pre-registered substitute. Whether the spectrum has a
clean gap or decays smoothly (where any rank tolerance is partly a statement about where the
analyst put it) is \citet{Gutenkunst_2007}'s general objection; Section~\ref{sec:experiments}
measures rather than assumes an answer. A learned summary statistic or neural posterior
estimator's embedding would enter here too, in place of $S_A$/$S_B$.
\item \textbf{Exact inference under a composite, simulation-only null.}
Section~\ref{sec:negative-result} evaluates a natural construction for turning the diagnostic's
output into a calibrated test: exact finite-sample inference under a null depending on unknown
nuisance parameters, by maximizing a simulated $p$-value over the nuisance set, is
\citet{Dufour_2006}'s maximized Monte Carlo test; calibrating a selection event with no
analytic description, by rejection-sampling the simulator's own null until a replicate lands in
the observed cell, is available for arbitrary conditioning events following
\citet{freidling2026selectiverandomizationinferenceadaptive}. Composing the two lets a
simulator supply everything both need without further modelling assumptions. This differs from
the foundational post-selection inference line motivating conditioning on a selection event at
all --- \citet{fithian2017optimalinferencemodelselection}, \citet{Lee_2016} --- in exactly the
respect Section~\ref{sec:negative-result} turns on: those constructions derive the conditional
law analytically for a specific procedure, where ours has no analytic characterisation and is
calibrated by simulation instead.
\end{itemize}

\section{Method}
\label{sec:method}

\paragraph{Scope assumption, stated first.}
Throughout this paper we assume each simulator component is misspecified by at most one
mechanism at a time: every component carries a single one-parameter distortion family, never
two at once. This is load-bearing rather than a footnote (Section~\ref{sec:experiments} shows
what happens once it is dropped), so we state it before the diagnostic's machinery.

\paragraph{The diagnostic.}
In four steps:
\begin{enumerate}[topsep=1pt,itemsep=1pt,parsep=0pt,leftmargin=*]
\item \textbf{Declare.} The simulator as $K$ components, each carrying a one-parameter distortion
family $\delta_k(\cdot\,;\eta_k)$ bit-identical to the base simulator at $\eta_k=0$, asserted
bit-for-bit before anything else runs.
\item \textbf{Simulate.} Estimate the summary Jacobian $J=\partial s/\partial\eta$ at $\eta=0$ by
central differences under common random numbers, swept over six decades of step size $h$;
$s\in\mathbb{R}^d$ is the summary vector, so $J$ is $d\times K$;
without common random numbers the difference is dominated by noise of order $\sigma/h$ and no
stable range exists (confirmed by a negative control).
\item \textbf{Normalise and take the SVD.} Summaries are divided by their prior-predictive
standard deviation and each $\eta_k$ by a fixed relative perturbation scale (both, and every
tolerance below, fixed before any singular value existed). The diagnostic's only input is the
summary vector; it never receives a component identity or a ground-truth label, so there is no
hidden truth available to leak into its answer.
\item \textbf{Decide.} Components are \emph{separable} if $J$'s condition number satisfies
$\kappa \le \kappa_{\max}=1/\tau$: equivalently, full column rank at tolerance $\tau$, since
$\kappa \le 1/\tau$ restates rank deficiency at $\tau$ rather than adding a second requirement;
we report both because each is the more natural unit in different places below. We use
$\tau=10^{-2}$, $\kappa_{\max}=100$, from a compute budget: resolving components to precision
$\kappa$ costs order $\kappa^2$ replicates (a singular value as small as $\sigma_1/\kappa$
needs the per-entry standard error below it, and that error falls as
$(\text{replicates})^{-1/2}$); $\kappa=100$ exhausts this study's budget. A singular value
varying by more than a factor of two across the plateau is \emph{unresolved} and counted toward
the rank in neither direction, a third outcome distinct from separable/not, so the reported rank
can be an interval rather than a single number. Resolution is a property of the
\emph{estimator}; whether the underlying spectrum has a clean gap or decays smoothly across many
decades is a property of the \emph{matrix}, and passing the resolution test answers the first
question, not the second (Section~\ref{sec:background}).
\end{enumerate}

\paragraph{Where separability fails.}
When the rank condition fails, we report the \emph{equivalence class} named by each near-null
right singular vector $v$ ($\sigma \le \tau\sigma_1$) rather than a single culprit: component
$k$ joins if $|v_k|\ge 0.3$, comfortably below both the two-component ($\approx 0.707$) and
three-component ($\approx 0.577$) confound values. No arrow leaves the verdict: the diagnostic
decides whether attribution is well posed, and nothing further.

\section{Experiments}
\label{sec:experiments}

\paragraph{Simulator and summary sets.}
Every number below traces to a results file and an exact dotted path, listed in full in
Appendix~\ref{sec:claims-ledger}. We apply the diagnostic to a $K=3$ compartmental epidemic simulator: transmission,
progression, observation (full structure and formulas: Figure~\ref{fig:simulator},
Appendix~\ref{sec:appendix}). Each component carries one one-parameter distortion family
$\delta_k(\cdot\,;\eta_k)$, $\eta_k$ in units of a fixed $10\%$ relative deformation: the
\emph{base} set is three qualitatively different deformations (a prevalence nonlinearity, a
timing distortion, an amplitude error); the \emph{adversarial} set is designed so all three
columns perturb the same feature, the epidemic curve's growth rate. Concretely: transmission's
base/adversarial pair is a prevalence-saturating nonlinearity and a constant multiplier on the
transmission rate; progression's is a time-drifting removal hazard and a constant multiplier on
the recovery rate; observation's is a constant reporting-fraction error and a time-drifting
reporting fraction; only the reporting fraction moves. We evaluate three summary sets: $S_A$, epidemic-curve features a
domain modeller would choose (peak height, peak time, final size, growth rate); $S_B$, roughly
ten time-binned incidence counts an SBI practitioner would choose; and $S_C$, an impoverished
positive control with two summaries, which \emph{must} fail by construction ($d=2<K=3$) and
does: rank 2, $\kappa=\infty$.

\paragraph{Where attribution is identifiable: $S_B$ under eight family assignments.}
Combining the two declared families component-wise gives eight three-column distortion models
(Figure~\ref{fig:assignments}), only two of which (all-base and all-adversarial) were
originally declared; the other six recombine the same columns.
Under our scope assumption, $S_B$ separates all three components under \emph{all eight}:
condition number $6.628$ to $65.64$, every singular value resolved, coherence below the
pre-registered $0.98$ flag and column norms above the pre-registered $0.1$ floor throughout.
$S_A$ is a control, not a result: it splits exactly on the transmission family, failing at
$\kappa=100.9$, $0.9\%$ past the ceiling, proof the instrument is not vacuously ``separable.''
The margin is not uniform: $S_B$'s verdict survives $\tau$ moving by
$9.881\times$ under the base assignment but only $1.523\times$ under the tightest one
(Table~\ref{tab:sb-eight}, Appendix~\ref{sec:appendix}), so doubling $\tau$ flips it. Nor is it
an artifact of the flat $10\%$ ETA\_SCALE
unit: rescaling by each parameter's confidence-set standard error instead, using only the three
coordinates the eta columns map onto ($\beta,\gamma,\rho$; $I_0$ and the noise scale are not
columns here)
(Section~\ref{sec:negative-result}: $0.9$--$2.7\%$, not a common $10\%$) leaves both separable
($\kappa=31.9$, $8.0$; rank $3$ of $3$).

\begin{figure}[tb]
  \centering
  \includegraphics[width=0.78\linewidth]{../figures/fig4_assignments.pdf}
  \caption{Condition number $\kappa$ under all eight component-wise family assignments
  (three-letter code: transmission/progression/observation, B~base, A~adversarial), for $S_B$
  (left) and $S_A$ (right), on a shared $\kappa$ axis, against the pre-registered ceiling
  $\kappa_{\max}=100$ (dashed). $S_A$ is a control, not a result: it rules out an instrument
  that always reports separable.}
  \label{fig:assignments}
\end{figure}

\paragraph{Where it is not: two distortion parameters per component.}
Placing all six declared distortion directions side by side (two per component) gives a
single six-column object. For $S_B$ it is \textbf{inseparable} at $\kappa=628.9$, rank $4$ of
$6$ (Figure~\ref{fig:spectrum}): not an unresolved estimator (worst singular-value variation
across the plateau, $1.023$ against an admissible $2$); not a threshold artifact (inseparable
across $\tau$ from $0.005$ to $1.0$, with no tolerance at which the object is both separable
and affordable); and not structural ($d=10\ge6$). Both near-null directions are
cross-mechanism, naming progression together with observation, with transmission carrying
under $5\%$ of the energy in either (Figure~\ref{fig:confound}, Appendix~\ref{sec:appendix}):
a drifting removal hazard is nearly indistinguishable from a constant hazard change combined
with a drifting reporting rate. The confound needs progression to carry two
distortion parameters at once. That is why it cannot arise under our one-parameter-per-component
scope, and why that restriction, not a bound on $\kappa$, is the assumption's real content.
Under the data-motivated scaling above, $\kappa$ falls to $344.9$ but stays $3.4\times$ past
$\kappa_{\max}=100$ (rank $5$ of $6$, not $4$: one near-null direction now resolves). In the
six-column case the spectrum matches the gapless, no-clean-break picture
\citet{Gutenkunst_2007}'s sloppy-models literature describes (spread $2.80$ decades, gap
prominence $1.40$, $\tau\sigma_1$ falling inside the spectrum); in the three-column case it does
not. The reassurance a three-column threshold offers is a fact about having three columns, not
about the simulator.

\begin{figure}[tb]
  \centering
  \includegraphics[width=0.62\linewidth]{../figures/fig3_spectrum.pdf}
  \caption{Normalised singular-value spectrum of $S_B$'s summary Jacobian under the base and
  adversarial three-column families and their six-column union, against the pre-registered
  tolerance $\tau=10^{-2}$ (dashed). Both three-column spectra clear $\tau$ by an order of
  magnitude; the six-column union's $\tau$ falls inside the spectrum, between $\sigma_4$
  (above $\tau$) and $\sigma_5$ (below it, the first near-null direction): rank 4 of 6,
  $\kappa=629$. Every value shown is \emph{resolved} (main text), so the rank deficiency is a
  property of the matrix, not of the estimator.}
  \label{fig:spectrum}
\end{figure}


\section{Where exact conditional attribution becomes unaffordable, and why}
\label{sec:negative-result}

Composing Dufour's maximized Monte Carlo with rejection-sampling calibration
(Section~\ref{sec:background}) turns the diagnostic's identifiable case into a candidate for
exact conditional inference on which component gets attributed: affordable at a
known parameter, but unable to terminate once the nuisance parameters are only known within a
realistic neighborhood. The boundary is predicted, before it is measured, from
the ratio of nuisance-induced shift to observation noise, and the prediction holds. The
neighborhood at which it takes over is not an assumption we chose: it is narrower than the
uncertainty a maximum-likelihood fit to the simulator's own data reports, on every one of the
five nuisance coordinates. The composition controls the selective type-I error conservatively
rather than exactly (both the supremum over the nuisance space and the discrete Monte Carlo
p-value are sources of conservativeness), and its only theorem requires the selection rule to
be a fixed, deterministic function of the data with every admissible nuisance value keeping
the observed selection event reachable.

\paragraph{Affordable at a known parameter, unbounded over a data-implied nuisance set.}
At the true parameter $\theta_0$ the composition is comfortably affordable: the worst selection cell
holds $23\%$ of null draws, and one test costs $4.2\times10^5$ to $4.3\times10^7$ simulator
draws against a pre-registered gate of $10^8$, a clear pass. The question is whether that
affordability survives once the nuisance parameters (transmission and recovery rates, reporting
fraction, initial infections, observation noise scale) are only known to the precision real
data give them, not an assumed one. We answer this directly rather than assume
a box: fitting the simulator to one realized dataset by maximum likelihood and building a
Bonferroni-corrected $95\%$ confidence box from the Fisher information gives relative
half-widths from $2.3\%$ to $16.6\%$; every one wider than the $\pm0.75\%$ box already known
to break the composition (below). Re-measuring the gate inside this box, the
acceptance probability collapses to $10^{-5}$ at the primary case and to zero acceptances in
$100{,}000$ draws at the other three declared combinations: the gate \textbf{fails at every
corner under all four}, the
cheapest needing $9.9\times10^9$ draws, approximately two orders of magnitude over budget. The collapse below
was measured under a round, assumed $\pm5\%$ box; here it survives, by a wide margin, under the
data-implied one.

\paragraph{The mechanism, and the shape of the boundary.}
The selection rule's fixed-function requirement means it cannot track $\theta$: nuisance
parameters shift the normalized summary vector while a single draw's noise magnitude stays
fixed, and once that shift exceeds the noise, a $\theta$-free rule selects one component
deterministically and the cell the data selected becomes unreachable. A sweep over ten relative
half-widths from $0.1\%$ to $5\%$
(Figure~\ref{fig:nontermination}) locates the shape of this collapse under the earlier,
round-number box: gradual rather than abrupt (log-slope ratio against a pre-registered
threshold of 3; measured $2.265\times$), about one decade of acceptance probability lost per
$0.7\%$ of relative nuisance error, log-linear at $R^2=0.94$, passing at every declared
corner only inside a $\pm0.5\%$ box, tighter than the data-implied box above supports. The
smallest round box at which the gate first fails ($\pm0.75\%$) sits at Mahalanobis radius
$3.56$ under the Fisher information at $\hat\theta$, against a joint $95\%$ ellipsoid radius
$\sqrt{\chi^2_{5,0.95}}\approx3.33$; the last fully-passing box ($\pm0.5\%$) sits at $2.71$. The
failure boundary and a real $95\%$ confidence region coincide almost exactly, sharper than
failing inside a Bonferroni box that only bounds the ellipsoid from outside.

\begin{figure}[h!]
  \centering
  \includegraphics[width=0.92\linewidth]{../figures/fig6_nontermination.pdf}
  \caption{Left: acceptance probability $p_{\min}(w)$, primary case (AAA, studentised), over
  the pre-registered 42-point design (32 corners $+$ 10 axis endpoints of half-width $w$ about
  $\theta_0$) and the $K=3$ cells; 95\% Wilson bars, open triangle at $w=0.05$ an upper bound
  joined to nothing. Shaded band: cost gate $M\!\times\!N/p\leq10^8$ ($M\in\{10^3,10^4\}$,
  $N\in\{99,999\}$); the gate passes where the measured line sits inside or above the band and
  fails where it falls below it. Plain/BBB variants in Figure~\ref{fig:nontermination-variants}
  (Appendix~\ref{sec:appendix}). Right: the mechanism, shift-to-noise ratio vs.\ $w$; line at
  ratio $=1$, dashed where first crossed, where the gate stops passing.}
  \label{fig:nontermination}
\end{figure}

\section{Limitations}
\label{sec:limitations}

\begin{itemize}[topsep=1pt,itemsep=0pt,parsep=0pt,leftmargin=*]
\item \textbf{The scope assumption is not a formality.} Every positive result assumes
(Section~\ref{sec:method}) at most one distortion parameter per component, a real restriction:
the moment one component carries two, the same summary set that separated all three becomes
inseparable on a real confound. We have not tested two distortions of the \emph{same} component.
\item \textbf{The diagnostic is a local linearization.} The rank-and-condition-number
screen (Section~\ref{sec:method}) does not carry an estimated simulation-noise level into the
rank tolerance, derived instead from a compute budget. $J$ is a linearization at $\eta=0$ and one
parameter point: separability at $10\%$ is not directly implied by local linear separability. A
second $\theta$, drawn $\pm20\%$ relative per coordinate, stays separable ($\kappa=10.9$, full
rank, matching $\theta_0$'s $\kappa=10.1$) --- one point, not a distribution.
\item \textbf{One simulator, one scope, no learned component.} Every result is on one simulator
under two family sets, every combination they permit rather than a general sample; transport to
other simulator classes or a learned summary statistic is untested. Two figures carry
hand-placed annotations outside the automated check.
\end{itemize}

\bibliographystyle{plainnat}
\bibliography{../audit/BIBLIOGRAPHY}

\appendix
\section{Appendix}
\label{sec:appendix}

\subsection{Compact overview of the paper's four findings}

Moved here from Section~\ref{sec:intro} for space; every entry is stated and evidenced in full
in Sections~\ref{sec:experiments}--\ref{sec:negative-result}, and this table adds no claim not
already in the main text.

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{0.9}
\hyphenpenalty=10000
\begin{tabular}{p{0.20\linewidth}p{0.13\linewidth}p{0.14\linewidth}p{0.30\linewidth}c}
\toprule
finding & object & key number & holds / fails where & fig. \\
\midrule
attribution identifiable & $S_B$, 3 columns & $\kappa$: 6.6--65.6 &
  all 8 one-parameter-per-component assignments tried & 1 \\
\dots\ stops being so & $S_B$, 6 columns & $\kappa=629$, rank 4/6 &
  once a component carries 2 distortion parameters & 2 \\
selective-inference test affordable & known $\theta$ & worst cell $p=0.23$ &
  only at a known parameter & 3a \\
\dots\ fails to terminate & data-implied nuisance box & worst cell $p=10^{-5}$ &
  once $\theta$ is known only to data-implied precision, predicted before measured & 3 \\
\bottomrule
\end{tabular}
\caption{The paper's four findings. ``Key number'' is the headline statistic; ``fig.''
points to the evidence figure.}
\label{tab:summary}
\end{table}

\subsection{Simulator structure}

Moved here from Section~\ref{sec:experiments} for space; the qualitative description of what
the base and adversarial distortion families do is restated in that section's own prose, so
nothing about the paper's claims depends on this figure appearing in the main text: what it
adds is the compartment flow and the exact distortion-family formulas, for reproducibility.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.85\linewidth]{../figures/fig2_simulator.pdf}
  \caption{Structure of the three-component compartmental simulator, and where each distortion
  family acts. There is no explicit removed compartment because nothing downstream observes
  one; the observation noise must be continuous and multiplicative rather than count-valued,
  or the common-random-numbers finite difference the diagnostic depends on does not exist.
  Each component carries one one-parameter distortion family $\delta_k(\cdot\,;\eta_k)$,
  bit-identical to the base simulator at $\eta_k=0$ (family descriptions:
  Section~\ref{sec:experiments}); under the observation family only the reporting fraction
  moves, with the delay kernel and noise scale held at base values, a stated design limitation.}
  \label{fig:simulator}
\end{figure}

\FloatBarrier

\subsection{The threshold-sensitivity sweep, the six-column confound, and the MMC variant
comparison, in full}

The first two are moved here from the main text for space; both are referenced from
Section~\ref{sec:experiments} and neither carries a claim not already stated in the main
text's prose. The third is the plain-studentisation and BBB-assignment comparison
Figure~\ref{fig:nontermination}'s main-text panel omits in favour of the primary case alone.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.85\linewidth]{../figures/fig5_threshold.pdf}
  \caption{Separability verdict for $S_B$ as a function of rank tolerance $\tau$
  ($\kappa_{\max}=1/\tau$). Vertical ticks mark each lane's exact flip point
  $\tau^*=\sigma_K/\sigma_1$; the dashed line is the registered $\tau=10^{-2}$ and the shaded
  band is a halve-and-double robustness check. The adversarial margin (1.55$\times$) is far
  tighter than the base margin (9.88$\times$), so doubling $\tau$ flips it; the six-column
  union stays inseparable across four decades of $\tau$.}
  \label{fig:threshold}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.85\linewidth]{../figures/fig7_confound.pdf}
  \caption{The two near-null directions of the six-column $S_B$ Jacobian. (a, b) Right
  singular vectors $v_5,v_6$ by distortion column, dashed line at the equivalence-class
  threshold $|v_k|=0.3$; (c) mechanism energy per direction. Both directions are cross-mechanism,
  naming progression together with observation, with transmission carrying under 5\% of the
  energy in either: a drifting removal hazard is nearly indistinguishable from a constant
  hazard change combined with a drifting reporting rate.}
  \label{fig:confound}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.62\linewidth]{../figures/fig6b_nontermination_variants.pdf}
  \caption{Acceptance probability $p_{\min}(w)$, with 95\% Wilson bars, across all four
  (family assignment, studentisation variant) combinations the composition was measured
  under, on the same 42-point design and axes as Figure~\ref{fig:nontermination}'s left
  panel. The primary case (AAA, studentised) is reproduced here for comparison. BBB,
  studentised and BBB, plain first reach zero measured acceptances earlier ($w=0.03$ and
  $w=0.02$) than the primary case ($w=0.05$); AAA, plain never reaches a measured zero
  anywhere in this sweep. Open triangles are 95\% Wilson upper bounds at zero measured
  acceptances, not values, and are joined to nothing.}
  \label{fig:nontermination-variants}
\end{figure}

\FloatBarrier

\subsection{The eight family assignments, in full}

\input{appendix_tables.tex}

\FloatBarrier

\subsection{Every number this paper's claims rest on, and its source}
\label{sec:claims-ledger}

Every number the main text's four contributions and the confidence-set check of
Section~\ref{sec:negative-result} rest on, generated programmatically from the underlying
measurement files rather than typed by hand, each with the exact file and path it was read
from.

\input{appendix_claims_table.tex}

\clearpage
\section*{Reproducibility checklist}
\input{checklist.tex}

\end{document}
